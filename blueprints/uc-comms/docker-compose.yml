version: "3.8"

services:
  uc-comms:
    build:
      context: .
      dockerfile: Dockerfile
    restart: unless-stopped
    ports:
      - "3000"
    volumes:
      - uc-comms-data:/app/data

volumes:
  uc-comms-data: {}