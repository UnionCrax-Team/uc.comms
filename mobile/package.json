{
  "name": "@uc-comms/mobile",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "cap:add:android": "cap add android",
    "cap:add:ios": "cap add ios",
    "cap:sync": "cap sync",
    "cap:open:android": "cap open android",
    "cap:open:ios": "cap open ios",
    "cap:build:android": "cap build android",
    "cap:build:ios": "cap build ios",
    "add": "npm run cap:add:android && npm run cap:add:ios",
    "build": "npm run cap:build:android && npm run cap:build:ios"
  },
  "dependencies": {
    "@capacitor/android": "^7.0.0",
    "@capacitor/core": "^7.0.0",
    "@capacitor/ios": "^7.0.0"
  },
  "devDependencies": {
    "@capacitor/cli": "^7.0.0",
    "@types/node": "^22.10.5",
    "typescript": "^5.7.2"
  }
}
